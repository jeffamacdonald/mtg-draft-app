<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav ml-auto">
        <% if user_signed_in? %>
            <li class="nav-item">
                <%= render(ButtonComponent.new(url: drafts_path, classes: "nav-link", data: { turbo_method: :get })) { "Drafts" } %>
            </li>
            <li class="nav-item">
                <%= render(ButtonComponent.new(url: cubes_path, classes: "nav-link", data: { turbo_method: :get })) { "Cubes" } %>
            </li>
            <li class="nav-item">
                <%= render(ButtonComponent.new(url: destroy_user_session_path, classes: "nav-link", data: { turbo_method: :delete })) { "Log out" } %>
            </li>
        <% else %>
            <li class="nav-item">
                <%= link_to 'Sign In', new_user_session_path, class: 'nav-link' %>
            </li>
            <li class="nav-item">
                <%= link_to 'Register', new_user_registration_path, class: 'nav-link' %>
            </li>
        <% end %>
    </ul>
  </div>
</nav>
